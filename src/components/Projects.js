import React from "react";

const Projects = () => {
    return(
        <div style={{display:'flex', backgroundColor:'black', justifyContent:'center', alignItems:'center'}}>
            <h1 style={{color: 'white'}}>This is the Project Page</h1>
        </div>
    )
};

export default Projects;